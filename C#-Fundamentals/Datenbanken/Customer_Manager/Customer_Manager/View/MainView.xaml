<Window x:Class="Customer_Manager.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Management" Height="520" Width="900"
        WindowStartupLocation="CenterScreen">

    <DockPanel Margin="10">

        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
            <Button Content="Add"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding AddCommand}"/>

            <Button Content="Edit"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding EditCommand}"/>

            <Button Content="Delete"
                    Width="90"
                    Margin="0,0,18,0"
                    Command="{Binding DeleteCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Street" Binding="{Binding Street}" Width="*"/>
                <DataGridTextColumn Header="No." Binding="{Binding HouseNumber}" Width="80"/>
                <DataGridTextColumn Header="ZIP" Binding="{Binding ZipCode}" Width="90"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </DockPanel>
</Window>