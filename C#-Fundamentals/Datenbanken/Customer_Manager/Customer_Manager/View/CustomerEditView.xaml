<Window x:Class="Customer_Manager.View.CustomerEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer" Height="420" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="First Name" Margin="0,0,10,8"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.FirstName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Last Name" Margin="0,0,10,8"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.LastName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Street" Margin="0,0,10,8"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.Street, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="House No." Margin="0,0,10,8"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.HouseNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="ZIP Code" Margin="0,0,10,8"/>
            <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.ZipCode, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="City" Margin="0,0,10,8"/>
            <TextBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.City, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Email" Margin="0,0,10,8"/>
            <TextBox Grid.Row="6" Grid.Column="1" Margin="0,0,0,8"
                     Text="{Binding Customer.Email, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="7" Grid.ColumnSpan="2"
                       Foreground="Red"
                       Text="{Binding ErrorMessage}"
                       Margin="0,6,0,0"/>
        </Grid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Content="Cancel"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Save"
                    Width="90"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>